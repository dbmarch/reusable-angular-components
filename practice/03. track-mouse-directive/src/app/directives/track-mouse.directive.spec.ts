import { TrackMouse } from './track-mouse.directive';

describe('TrackMouseDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackMouse();
    expect(directive).toBeTruthy();
  });
});
