import { Capitalize } from './capitalize';

describe('Capitalize', () => {
  it('should create an instance', () => {
    const directive = new Capitalize();
    expect(directive).toBeTruthy();
  });
});
